/**
 * Resume Configuration
 * ─────────────────────────────────────────────
 * To update your resume:
 *   1. Drop the new PDF into /public/resume.pdf
 *      (keep the filename as "resume.pdf" so you never have to touch code)
 *   2. Update RESUME_LAST_UPDATED below to today's date.
 *   3. Commit and push — done!
 *
 * If you ever want a different filename (e.g. after a big redesign):
 *   Change RESUME_PATH here instead of hunting through component files.
 */

export const RESUME_PATH = '/AndiScarola_Resume.pdf';

// Human-readable "last updated" label shown on the About page.
// Update this string each time you swap in a new resume.
export const RESUME_LAST_UPDATED = 'May 2025';

// Download filename shown in the browser's Save dialog.
export const RESUME_DOWNLOAD_NAME = 'AndiScarola_Resume.pdf';
